# Pre-commit hooks — ruff lint + format.
# O'rnatish: uv run pre-commit install
# Versiyani yangilash: uv run pre-commit autoupdate
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
